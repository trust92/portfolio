// lib/config.ts
// Configuration for API routes

export const IMAGE_DIR = './public/gallery';

// Optional Uses process.env directly instead of libconfig (excluded)
// import path from 'path';
// export const IMAGE_DIR = path.join(process.cwd(), process.env.IMAGE_DIR || '/public/gallery');